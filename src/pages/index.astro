---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
const posts = await getCollection('post');
---

<Layout title="Henry Percy">
	<main>
		<section class="space-y-5 prose prose-stone dark:prose-invert">
			<h1 class="mb-4 text-3xl font-bold leading-none tracking-tight md:text-4xl">Henry</h1>
			<p>Junior Software Developer <span class="text-black dark:text-white font-semibold">building the web</span>, one byte at a time, from <span class="text-black dark:text-white font-semibold">London</span>.</p>
			<p>Welcome to my <a href="https://xkcd.com/195/" target="_blank">corner of the internet</a>. This is a place where I document my adventures in software development, sharing the lessons, triumphs, and challenges that shape my journey. Dive in and discover the stories behind the code.</p>

			<p>I am writing a blog to document my journey into Software Development to reinforce my learning, and hopefully help others facing similar problems.</p>
			{posts
				.sort((a, b) => b.data.publishDate - a.data.publishDate)
				.slice(0, 3)
				.map(post => (
					<a href={`posts/${post.slug}`} class="flex items-center justify-between gap-x-2 py-2 px-4 group not-prose rounded border border-stone-400 dark:border-stone-700 bg-stone-200 hover:bg-stone-300 hover:text-black dark:bg-stone-900 hover:dark:bg-stone-800 hover:dark:text-white transition-all ">
						<div>
							<p href={`/posts/${post.slug}`}>{post.data.title}</p>
							<p class="text-sm text-stone-500">{post.data.publishDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}</p>
						</div>
						<svg class="shrink-0 fill-stone-700 dark:fill-stone-300 group-hover:fill-black group-hover:dark:fill-white transition-all duration-500" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24"><path d="m16 8.4l-8.9 8.9q-.275.275-.7.275t-.7-.275q-.275-.275-.275-.7t.275-.7L14.6 7H7q-.425 0-.713-.288T6 6q0-.425.288-.713T7 5h10q.425 0 .713.288T18 6v10q0 .425-.288.713T17 17q-.425 0-.713-.288T16 16V8.4Z"/></svg>
					</a>
				)
			)}
		</section>
	</main>
</Layout>
